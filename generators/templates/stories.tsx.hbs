import { Story, Meta } from '@storybook/react/types-6-0'

import {{pascalCase name}} from '.'

export default {
  title: '{{pascalCase category}}s/{{pascalCase name}}',
  component: {{pascalCase name}}
} as Meta

export const Default: Story = (args) => <{{pascalCase name}} {...args} />
